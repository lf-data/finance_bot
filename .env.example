# ── LLM (Docker container, OpenAI-compatible API) ─────────────────────────────
LLM_BASE_URL=http://localhost:8000/v1   # change to your container URL/port
LLM_MODEL=your-model-name              # model name exposed by the server
LLM_API_KEY=not-required               # placeholder – used only if server needs it
LLM_TEMPERATURE=0.2                    # lower = more deterministic
LLM_MAX_TOKENS=4096

# ── Tavily Search ──────────────────────────────────────────────────────────────
TAVILY_API_KEY=your_tavily_api_key     # https://app.tavily.com/

# ── Tickers to analyse ─────────────────────────────────────────────────────────
TICKERS=AAPL,MSFT,GOOGL,NVDA,TSLA

# ── Analysis parameters ────────────────────────────────────────────────────────
HISTORY_PERIOD=1y        # yfinance period: 6mo, 1y, 2y, 5y
HISTORY_INTERVAL=1d      # yfinance interval: 1d, 1wk
ANALYSIS_LANGUAGE=italian  # language for the final report (english|italian|...)

# ── Runtime ────────────────────────────────────────────────────────────────────
# How often to refresh the analysis (seconds). 86400 = once per day.
# Set to 0 to run once and exit.
LOOP_INTERVAL=86400

# ── Output ─────────────────────────────────────────────────────────────────────
REPORT_DIR=reports       # directory where HTML/text reports are saved
